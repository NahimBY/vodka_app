import 'package:flutter/widgets.dart';

class NewScreen extends StatelessWidget {
  static const String name = 'new_screen';

  const NewScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}